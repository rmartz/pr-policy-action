# @rmartz/pr-policy is public on npmjs and installs with no auth. Pin the scope
# explicitly: a user-level or runner-level .npmrc that maps @rmartz to GitHub
# Packages (still used by other @rmartz packages) would otherwise redirect
# `npm ci` there, where new versions are no longer published.
@rmartz:registry=https://registry.npmjs.org/
